{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "npm: compile",
      "type": "npm",
      "script": "compile",
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": "$tsc",
      "dependsOn": ["webview: build & sync"]
    },
    {
      "label": "webview: build & sync",
      "type": "shell",
      "command": "npm --prefix webview-ui run build ; node scripts/sync-v3-webview.mjs",
      "problemMatcher": [],
      "dependsOrder": "sequence"
    }
  ]
}
