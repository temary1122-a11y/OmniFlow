// NOTE: The real content of this file was moved to `postcss.config.host.js`.
// This file is intentionally neutralized (no plugins) so that if vite ever
// discovers it, it will NOT attempt to load `tailwindcss` (Tailwind v3 PostCSS
// syntax), which is not installed at the repo root. The webview UI uses
// Tailwind v4 via the `@tailwindcss/vite` plugin instead.
export default {};

